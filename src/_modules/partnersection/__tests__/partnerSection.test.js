'use strict';

import PartnerSection from '../partnersection';

describe('PartnerSection View', function() {

  beforeEach(() => {
    this.partnerSection = new PartnerSection();
  });

  it('Should run a few assertions', () => {
    expect(this.partnerSection);
  });

});
