'use strict';

import TeamSection from '../teamsection';

describe('TeamSection View', function() {

  beforeEach(() => {
    this.teamSection = new TeamSection();
  });

  it('Should run a few assertions', () => {
    expect(this.teamSection);
  });

});
